# 이번 주 말씀 카드 배경 이미지

이 폴더에 그 주의 배경 이미지를 넣으면 `weekly.html`이 자동으로 불러옵니다.

**파일 이름 규칙**: `assets/weekly-message-data.js`의 `weekLabel` 값과 정확히 같아야 합니다.
예: `weekLabel: "2026.08.30"` → 파일명 `2026.08.30.png`

파일이 없으면 기본 자연 배경(하늘~바다 그라데이션)이 대신 사용됩니다.
